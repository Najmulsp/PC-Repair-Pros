

const loading = () => {
    return (
        <div className="text-center min-h-screen">
            <span className="loading pt-40 loading-bars loading-lg text-orange-500"></span>
        </div>
    );
};

export default loading;